server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/network?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: Hars30896@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # use "create-drop" for fresh DB, "validate" for strict schema check
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    org.hibernate.SQL: debug
    org.hibernate.type.descriptor.sql.BasicBinder: trace
